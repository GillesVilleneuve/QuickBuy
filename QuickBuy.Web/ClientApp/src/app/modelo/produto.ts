export class Produto {

  id: number;  
  nome: string;
  descricao: string;
  preco: number;
  nomeArquivo: string;
  precoOriginal: number; // não precisar inserir essa propriedade na camada de domínio
  quantidade: number;

}
